export default function SectionPlaceholder({ title }) {
  return (
    <section className="section-placeholder">
      <h1>{title}</h1>
      <p>Esta sección todavía no tiene diseño propio.</p>
    </section>
  )
}
